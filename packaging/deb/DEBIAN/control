Package: ruyiseek
Version: 0.1.0-10
Section: utils
Priority: optional
Architecture: amd64
Maintainer: RuyiSeek contributors <ruyiseek@users.noreply.github.com>
Homepage: https://github.com/EthanBird/RuyiSeek
Vcs-Browser: https://github.com/EthanBird/RuyiSeek
Vcs-Git: https://github.com/EthanBird/RuyiSeek.git
Depends: libc6 (>= 2.28)
Recommends: systemd
Suggests: xdg-utils, libdbus-1-3, xclip, wl-clipboard
Description: 如意寻 — UOS/DDE 全局文件与应用启动器
 如意寻（RuyiSeek）是面向统信 UOS 与 Deepin（DDE）桌面的常驻本地
 文件搜索与全局启动器，提供双击 Ctrl 呼出、应用与文件统一搜索、
 系统托盘、设置中心和会话安全等能力。
 .
 本包提供阶段 A1 第 4 个纵向切片：
  * ruyi：命令行查询客户端（ping / status / search / stop）；
  * ruyiseekd：常驻搜索守护进程，监听 $XDG_RUNTIME_DIR 下 Unix Socket；
  * ruyiseek-ui：Slint 启动器，支持 D-Bus 单实例、托盘与双击 Ctrl；
  * systemd --user 单元、D-Bus 激活服务与 XDG 自动启动条目。
 .
 索引范围、收藏、拼音匹配、原生索引快照等能力将在后续版本中提供。
 .
 本包采用混合构建：
  * `ruyi`、`ruyiseekd` 完全静态链接（musl libc），零系统 C 库依赖；
  * `ruyiseek-ui` 动态链接到系统 glibc（Slint/winit 通过 dlopen 加载
    libX11.so.6 等 X11 客户端库，而 musl-static 的 dlopen 是桩函数）。
 .
 因此 `ruyiseek-ui` 需要运行时存在 libc6 / libgcc1 与若干 X11 库；
 `ruyi` 与 `ruyiseekd` 不依赖这些。安装路径只需 `dpkg -i ./xxx.deb`
 或 `apt install ./xxx.deb`，无需 `apt-get -f`。
 .
 v0.1.0-10 起 `ruyiseek-ui` 把 glibc 基础栈、X11 客户端栈、字体栈
 与动态链接器全部内联进 /usr/lib/ruyiseek/，二进制 PT_INTERP /
 DT_RPATH 已 patchelf 到该目录；离线（无 apt 仓库）机器上
 `dpkg -i ./xxx.deb` 即可，不依赖 apt 拉任何运行时依赖（仅 Depends
 `libc6 (>= 2.28)`，用于 ldconfig 与 dynamic loader 注册）。
Installed-Size: 13564
