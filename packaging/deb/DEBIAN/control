Package: ruyiseek
Version: 0.1.0-13
Section: utils
Priority: optional
Architecture: amd64
Maintainer: RuyiSeek contributors <ruyiseek@users.noreply.github.com>
Homepage: https://github.com/EthanBird/RuyiSeek
Vcs-Browser: https://github.com/EthanBird/RuyiSeek
Vcs-Git: https://github.com/EthanBird/RuyiSeek.git
Depends: libc6 (>= 2.28)
Recommends: systemd
Suggests: xdg-utils, libdbus-1-3
Description: 如意寻 — UOS/DDE 全局文件与应用启动器
 如意寻（RuyiSeek）是面向统信 UOS 与 Deepin（DDE）桌面的常驻本地
 文件搜索与全局启动器，提供双击 Ctrl 呼出、应用与文件统一搜索、
 系统托盘、设置中心和会话安全等能力。
 .
 本包提供阶段 A1 第 4 个纵向切片：
  * ruyi：命令行查询客户端（ping / status / search / stop）；
  * ruyiseekd：常驻搜索守护进程，监听 $XDG_RUNTIME_DIR 下 Unix Socket；
  * ruyiseek-ui：Slint 启动器，支持 D-Bus 单实例、托盘与双击 Ctrl；
  * systemd --user 单元、D-Bus 激活服务与 XDG 自动启动条目。
 .
 手工索引范围、收藏、拼音匹配、原生索引快照等能力将在后续版本中提供。
 .
 本包采用混合构建：
  * `ruyi`、`ruyiseekd` 完全静态链接（musl libc），零系统 C 库依赖；
  * `ruyiseek-ui` 动态链接到系统 glibc（Slint/winit 通过 dlopen 加载
    libX11.so.6 等 X11 客户端库，而 musl-static 的 dlopen 是桩函数）。
 .
 因此 `ruyiseek-ui` 只要求目标系统已有 libc6 基础栈；libgcc1、X11 与
 字体运行库随本包提供，`ruyi` 与 `ruyiseekd` 则为静态程序。安装只需
 `dpkg -i ./xxx.deb` 或 `apt install ./xxx.deb`，无需 `apt-get -f`。
 .
 v0.1.0-13 起 `ruyiseek-ui` 把 X11 客户端栈、字体栈与 C++ 运行库
 内联进 /usr/lib/ruyiseek/，并在链接阶段写入相对 DT_RPATH；glibc 与
 动态链接器使用 UOS 的 libc6 基础包。离线（无 apt 仓库）机器上
 `dpkg -i ./xxx.deb` 即可，不依赖 apt 拉任何运行时依赖（仅 Depends
 `libc6 (>= 2.28)`，它是 UOS 20 的系统基础包）。
 .
 v0.1.0-11 起 `ruyiseek-ui` 的"复制文件 / 复制路径"完全用纯 Rust
 实现的 X11 CLIPBOARD 协议，不再 spawn xclip/wl-copy；启动器结果
 列表最多露 10 行 + Flickable 滚动，搜索结果上限从 9 提到 50；
 查询引擎支持多关键词 AND、首字母 acronym 命中、按 / 切段的路径
 段位加权。
 .
 v0.1.0-12 起 `ruyiseekd` 会从 Linux mountinfo 自动发现 `$HOME` 之外
 的可读本地数据卷，并在运行期间检测挂载/卸载后后台重建、原子切换
 内存索引；系统树、loop、网络文件系统与重复 bind mount 默认排除。
 自动索引容量按每个根 25 万条扩展，总上限 200 万条。
 .
 v0.1.0-13 修复 UOS 发布包：共享库软链接与真实 ELF 文件会一起打包，
 并通过隔离动态加载器验证；构建不再依赖绝对路径、历史 target 缓存、
 musl-stage 或临时 shim.o，deb 中也不再混入构建辅助脚本。
Installed-Size: 0
